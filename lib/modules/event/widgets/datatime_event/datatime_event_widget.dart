import 'package:flutter/material.dart';

class DatatimeEventWidget extends StatelessWidget {
  const DatatimeEventWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}